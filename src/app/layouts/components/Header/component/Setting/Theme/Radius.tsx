import NumberItem from '../NumberItem';

/**
 * 圆角
 * @returns
 */
const Radius: React.FC = () => {
  return <NumberItem title="圆角" category="theme" pKey="radius" suffix="px" />;
};
export default Radius;
